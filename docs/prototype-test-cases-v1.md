# 原型测试用例 v1（编码前）

> 依据：`docs/wireframe-spec-v2.1.docx`（已冻结）｜ `docs/brd.md` ｜ `docs/requirements-baseline.md`
> 原则：**测试用例先于代码存在**。本文件在 M0 之前定稿，作为各批次的验收依据。
> 执行方式：手工执行 + 截图留证。每条用例的「禁止出现的结果」为**硬断言**，出现即判失败。

---

## 用例总览

| 编号 | 主题 | 对应 R | 批次 |
| --- | --- | --- | --- |
| TC-01 | 五项一级导航与 hash 深链接 | R-15 | M1 |
| TC-02 | 六角色页面权限 | R-15 | M1 |
| TC-03 | 字段级遮蔽与动作级禁用 | R-15 | M1/M4 |
| TC-04 | 服务商组织隔离 | R-15 | M1/M4 |
| TC-05 | 三条状态链独立显示 | R-10 | M4 |
| TC-06 | L4 左右栏常驻、中栏阶段切换 | R-02 | M2 |
| TC-07 | AI 草稿 / 人工稿 / 正式记录视觉隔离 | R-11/R-12 | M7 |
| TC-08 | AI 结论依据展开 | R-12 | M7 |
| TC-09 | 证据不足与 AI 不可用时的人工降级 | R-12 | M7 |
| TC-10 | C6 四项提交前置条件 | R-13 | M8 |
| TC-11 | R-A 四项正式记录准入信息 | R-11 | M3 |
| TC-12 | R-A1/R-A2/R-A3 三条独立正式路径 | R-11 | M3/M8 |
| TC-13 | v1/v2 版本并列保留 | R-11 | M3/M8 |
| TC-14 | W6 只查看定位，不形成正式结果 | R-08 | M5 |
| TC-15 | W5 创建、双方确认、非创建人审核生效 | R-03 | M6 |
| TC-16 | CONFIG_ACTIVATE 权限 | R-03/R-15 | M6 |
| TC-17 | 重复提交拦截与幂等键 | R-13 | M8 |
| TC-18 | 失败后从最近确认节点续办 | R-13 | M8 |
| TC-19 | 1440/1280/1100/900 四档响应式 | R-02 | M2/M9 |
| TC-20 | 900px 右栏摘要条与宽抽屉 | R-02 | M2/M9 |
| TC-21 | 五层记录不覆盖 | R-11 | M8/M9 |
| TC-22 | 事实核对按需阶段（R-07 专项） | R-07 | M2 |

---

## TC-01 · 五项一级导航与 hash 深链接

| 项 | 内容 |
| --- | --- |
| **对应 R** | R-15 |
| **前置角色** | **京东资料核查人员（李强）** —— 该角色可见全部五项一级导航。<br>其余角色为权限子集，由 TC-02 覆盖 |
| **前置数据** | 无（仅需页面可加载） |
| **操作步骤** | 1. 加载 `prototype.html`<br>2. 观察侧边栏导航项数量与名称<br>3. 依次访问 `#l1` `#l2` `#l3` `#w6` `#w5`<br>4. 访问 `#w4/JD202409130001/recon`<br>5. 访问 `#w6?period=2024-09&state=diff` |
| **预期结果** | 导航恰为 **5 项**：角色工作台 / 任务队列 / 工单中心 / **业务对账工作区** / 接入配置工作区；每条深链接均直接打开对应页面且浏览器地址栏同步；`#w4/<工单号>/<阶段>` 能定位到对应阶段；W6 的 `period` 与 `state` 参数被读取并反映在筛选器上 |
| **禁止出现的结果** | ① 导航项 ≠ 5 项（多出「数据看板」「标准费用库」等 v1.0 遗留项，或缺少 W6）<br>② 深链接打开后落在默认首页<br>③ 业务对账无独立一级入口、只能经工单进入<br>④ 带查询参数的深链接（`#w6?period=…`）解析失败回落到首页 |
| **所属批次** | M1 |

---

## TC-02 · 六角色页面权限

| 项 | 内容 |
| --- | --- |
| **对应 R** | R-15 |
| **前置角色** | 依次切换六个角色 |
| **前置数据** | 六个账号：李强（京东资料核查人员）/ 周明（京东业务对账人员）/ 陈静（接入配置管理员）/ 王磊（系统管理员）/ 张三（服务商资料负责人）/ 刘敏（服务商业务负责人） |
| **操作步骤** | 逐个角色切换，记录侧边栏实际出现的导航项 |
| **预期结果** | 服务商侧两角色**不出现**「接入配置工作区」；接入配置管理员**不出现**「任务队列」；其余角色按 v2.1 第 11 部分权限矩阵呈现；无任何角色能通过深链接绕过（直接访问 `#w5` 时被重定向回该角色工作台并给出提示） |
| **禁止出现的结果** | ① 任何角色看到全部五项而不受限<br>② 通过深链接绕过页面级权限<br>③ 出现**第七个角色**或「超级管理员」类万能角色 |
| **所属批次** | M1 |

---

## TC-03 · 字段级遮蔽与动作级禁用

| 项 | 内容 |
| --- | --- |
| **对应 R** | R-15 |
| **前置角色** | 接入配置管理员（陈静）、服务商资料负责人（张三） |
| **前置数据** | 工单 `JD202409130001`，服务商应收 960.00，京东账单 900.00，差额 60.00 |
| **操作步骤** | 1. 以陈静进入 L4，观察左栏金额字段与右栏 R-A3<br>2. 以张三进入 L4，观察 R-A3 与核查结论<br>3. 观察二者中栏可执行动作 |
| **预期结果** | 陈静：左栏业务金额**被遮蔽**、R-A3 **不可见**、中栏无工单业务动作可执行<br>张三：可见 R-A1 本单、R-A2 **只读且不可确认**、R-A3 **不可见** |
| **禁止出现的结果** | ① 接入配置管理员看到具体金额（960.00 / 900.00 / 60.00）<br>② 服务商资料负责人看到 R-A3 对账确认结果<br>③ 服务商资料负责人能点击「确认」京东正式核查结论<br>④ 用红色错误提示表达权限不足（应为禁用 + 说明） |
| **所属批次** | M1 / M3 / M4 |
| **当前状态** | ⚠️ **部分通过**（M3 更新）<br>**已通过**：<br>· 右栏区块级可见性——服务商资料负责人与接入配置管理员的 R-A3 **整块从 DOM 剥离**（非 CSS 隐藏）<br>· 金额遮蔽——系统管理员/接入配置管理员的正式记录金额显示为 `¥ ••••`<br>· 摘要条与宽抽屉沿用同一权限，折叠后不泄露<br>**待后续批次**：<br>· 左栏字段级遮蔽（M4）<br>· C6 正式动作禁用（M8）<br>· 服务商资料负责人「不得确认京东正式核查结论」的动作禁用（M4） |

---

## TC-04 · 服务商组织隔离

| 项 | 内容 |
| --- | --- |
| **对应 R** | R-15 |
| **前置角色** | 服务商资料负责人（张三，属**城南安装**） |
| **前置数据** | 工单：`JD202409130001`（城南安装）、`JD202409120014`（远程机电）、`JD202409110008`（安家安装） |
| **操作步骤** | 1. 以张三进入 L3 工单中心<br>2. 清空全部筛选，查看完整列表<br>3. 以京东资料核查人员（李强）重复步骤 1-2 |
| **预期结果** | 张三**只看到城南安装的工单**（1 条）；李强看到全部 3 条 |
| **禁止出现的结果** | ① 服务商侧看到其他服务商的工单号、项目、金额<br>② 组织隔离靠前端筛选实现（应作为数据层硬隔离，前端筛选清空后仍不可见） |
| **所属批次** | M1 / M4 |

---

## TC-05 · 三条状态链独立显示

| 项 | 内容 |
| --- | --- |
| **对应 R** | R-10 |
| **前置角色** | 京东业务对账人员（周明） |
| **前置数据** | 工单 `JD202409130001`：现场履约=已完工、资料交接=待补充、对账处理=有差异 |
| **操作步骤** | 1. 进入 L3，观察表格列<br>2. 进入 L4，观察左栏三状态条<br>3. 全局搜索「总状态」「工单状态」字样 |
| **预期结果** | L3 为**三个独立列**（现场履约 / 资料交接 / 对账处理）；L4 左栏为**三段等宽、竖线分隔、各自独立配色**的状态条；三段可点击分别跳对应阶段 |
| **禁止出现的结果** | ① 出现「工单总状态」「整体状态」类合并列或合并胶囊<br>② 三状态被渲染成同一背景的单一控件<br>③ 出现「进行中」「已完成」等把三条链合并的措辞 |
| **所属批次** | M4 |

---

## TC-06 · L4 左右栏常驻、中栏阶段切换

| 项 | 内容 |
| --- | --- |
| **对应 R** | R-02 |
| **前置角色** | 京东资料核查人员（李强） |
| **前置数据** | 工单 `JD202409130001` |
| **操作步骤** | 1. 进入 `#w4/JD202409130001/handover`<br>2. 依次点击阶段导航中的「核查」「对账」<br>3. 每次切换后观察左右两栏内容 |
| **预期结果** | 中栏在「资料交接 / 核查 / 对账」之间切换；**左栏与右栏内容全程不变**（工单标识、三状态、事实分组、R-A 三区块均保持） |
| **禁止出现的结果** | ① 切换阶段时左右栏内容随之变化<br>② 切换阶段时页面整体刷新或滚动位置重置到顶部以外<br>③ 出现第六种阶段或把「接入配置」当作画布阶段 |
| **所属批次** | M2 |

---

## TC-07 · AI 草稿 / 人工稿 / 正式记录视觉隔离

| 项 | 内容 |
| --- | --- |
| **对应 R** | R-11、R-12 |
| **前置角色** | 服务商资料负责人（张三） |
| **前置数据** | 工单 `JD202409130001` 资料交接阶段，含 AI 整理草稿 v1、人工确认稿 v1、正式提交版本 v1 |
| **操作步骤** | 1. 进入资料交接阶段，观察三层的边框、底色、角标<br>2. 观察每层可用的动作按钮<br>3. 观察右栏 R-A1 |
| **预期结果** | 草稿 = **虚线边框 + 斜纹底 + amber 圆点角标「AI 草稿」**，动作仅「采纳 / 丢弃」；人工确认稿 = 实线细边 + blue 角标「人工确认」；正式版本 = 实线 + **左侧 3px 主色竖条** + green 角标「正式提交 v1」+ 回执号 |
| **禁止出现的结果** | ① AI 草稿出现在右栏 R-A 区块<br>② 草稿容器内出现「提交」「正式」字样按钮<br>③ 用绿色（本系统专属于"已通过原系统校验"）表达草稿<br>④ 草稿被渲染成表格行而非斜纹容器 |
| **所属批次** | M7 |

---

## TC-08 · AI 结论依据展开

| 项 | 内容 |
| --- | --- |
| **对应 R** | R-12 |
| **前置角色** | 任意可查看资料交接阶段的角色 |
| **前置数据** | AI 整理草稿 v1，含 3 条依据 |
| **操作步骤** | 1. 在草稿容器底部找到「依据 3 条 ▸」<br>2. 点击展开<br>3. 逐条检查四要素 |
| **预期结果** | 从右侧滑出 **D1 窄抽屉（约 420px）**，不离开当前上下文；每条依据均含**来源系统 / 文件或字段 / 位置 / 摘录原文**四要素（如「文件 CN-2024-0912-018 · 第 3 行 · D3:E3」） |
| **禁止出现的结果** | ① 依据只有一句笼统说明而无位置信息<br>② 点击依据跳转新页面或关闭当前上下文<br>③ 抽屉宽度与宽抽屉相同（应为窄抽屉） |
| **所属批次** | M7 |

---

## TC-09 · 证据不足与 AI 不可用时的人工降级

| 项 | 内容 |
| --- | --- |
| **对应 R** | R-12 |
| **前置角色** | 服务商资料负责人（张三） |
| **前置数据** | ① 草稿含 1 项缺失证据、1 项待确认问题<br>② 模拟 AI 调用失败 |
| **操作步骤** | 1. 观察草稿容器底部的缺失证据与待确认问题<br>2. 尝试折叠它们<br>3. 触发 AI 失败，观察 C4 区域<br>4. 在降级状态下完成一次资料整理 |
| **预期结果** | 缺失证据（红）/ 待确认问题（黄）**固定在容器底部且不可折叠**；AI 失败时 C4 折叠为**可完成同样工作的表单**（三列：本地字段 / 程序转换结果 / 人工填写），并显示统一提示条 `⚠ AI 暂不可用…[重试 AI]`；程序转换结果**照常显示** |
| **禁止出现的结果** | ① 缺失证据可被折叠或隐藏<br>② AI 失败仅显示一条错误提示、无法继续作业<br>③ AI 失败导致程序转换结果一并消失<br>④ 重试时容器闪烁或布局跳动 |
| **所属批次** | M7 |

---

## TC-10 · C6 四项提交前置条件

| 项 | 内容 |
| --- | --- |
| **对应 R** | R-13 |
| **前置角色** | 服务商资料负责人（张三，具有资料提交权限） |
| **前置数据** | ① 无人工确认稿的工单<br>② 人工确认稿存在但确定性校验不通过（3 项）<br>③ 校验通过但幂等键重复<br>④ 三项均满足 |
| **操作步骤** | 依次构造上述四种状态，观察 C6「当前阶段正式动作区」的提交按钮 |
| **预期结果** | ① **存在完整的人工确认稿** ② **确定性校验通过** ③ **当前角色具有正式操作权限** ④ **幂等键未重复** —— 四项全满足才可点击；任一项不满足则按钮**禁用并逐条列出未满足项** |
| **禁止出现的结果** | ① 三项满足即可提交<br>② 校验不通过时仍可通过<br>③ 幂等键重复时产生第二条正式记录<br>④ 把「程序校验通过」等同于「存在人工确认稿」 |
| **所属批次** | M8 |

---

## TC-11 · R-A 四项正式记录准入信息

| 项 | 内容 |
| --- | --- |
| **对应 R** | R-11 |
| **前置角色** | 京东资料核查人员（李强） |
| **前置数据** | ① 提交中（等待回执）<br>② 提交超时<br>③ 原系统拒绝<br>④ 已收到成功回执<br>⑤ 对账有差异处理中<br>⑥ 对账已正式确认 |
| **操作步骤** | 逐一构造上述状态，观察右栏 R-A1 / R-A3 |
| **预期结果** | 仅 ④⑥ 进入右栏，且必须同时具备：**已正式形成 / 有回执号或版本号 / 有操作人 / 有形成时间**；①②③⑤ 只显示「**尚未形成正式记录**」+「**前往当前阶段**」 |
| **禁止出现的结果** | ① 右栏显示「等待回执」「提交中」「处理中」「待确认」<br>② 右栏显示「有差异·处理中」或「差额 60 元」等过程状态与金额<br>③ 三个区块合并为一个空态<br>④ 缺少回执号或操作人时间的结果进入右栏 |
| **所属批次** | M3 |

---

## TC-12 · R-A1/R-A2/R-A3 三条独立正式路径

| 项 | 内容 |
| --- | --- |
| **对应 R** | R-11 |
| **前置角色** | 张三（资料）→ 李强（核查）→ 周明（对账） |
| **前置数据** | 工单 `JD202409130001` 处于端到端流程中 |
| **操作步骤** | 1. 完成资料正式提交 → 观察 R-A1<br>2. 进入核查阶段并形成正式核查结论 → 观察 R-A2<br>3. 进入对账阶段并完成对账正式确认 → 观察 R-A3<br>4. 观察三者是否互相覆盖 |
| **预期结果** | 三条路径**各自独立**：R-A1 在收到京东安装管理系统回执后点亮；R-A2 经核查阶段 C6 形成（或原系统已形成时 C6 呈只读同步态）；R-A3 经对账阶段 C6 形成。**三者互不覆盖，也不汇总为工单总状态** |
| **禁止出现的结果** | ① 三条路径合并为一个「工单已完成」状态<br>② 形成 R-A2 时 R-A1 内容消失<br>③ 某条路径跳过该阶段的 C6 直接写入右栏<br>④ 出现「始终只有一处产生正式记录」类合并表述 |
| **所属批次** | M3 / M8 |

---

## TC-13 · v1/v2 版本并列保留

| 项 | 内容 |
| --- | --- |
| **对应 R** | R-11 |
| **前置角色** | 张三（补充）→ 李强（复核） |
| **前置数据** | 工单已完成 v1 全链，随后服务商补充材料产生 v2 全链 |
| **操作步骤** | 1. 构造补充资料场景（原始资料 v2 → 程序转换结果 v2 → AI 草稿 v2 → 人工确认稿 v2 → 正式提交版本 v2）<br>2. 打开 D2「处理过程与版本」<br>3. 检查五层各自的 v1 与 v2 |
| **预期结果** | 五层**均出现 v1 与 v2 并列**，各自独立保存；每版标注时间与操作人；v1 完整可查 |
| **禁止出现的结果** | ① 只有人工确认稿出现 v2、其余层仍显示 v1（链路不完整）<br>② v2 覆盖 v1<br>③ v1 从界面上消失<br>④ AI 草稿 v2 缺失时被判定为链路断裂（该项为**可选**） |
| **所属批次** | M3 / M8 |

---

## TC-14 · W6 只查看定位，不形成正式结果

| 项 | 内容 |
| --- | --- |
| **对应 R** | R-08 |
| **前置角色** | 京东业务对账人员（周明） |
| **前置数据** | 账期 2024-09，4 笔待对账，其中 2 笔有差异 |
| **操作步骤** | 1. 进入 `#w6`<br>2. 检查页面上所有可点击元素<br>3. 点击一条差异<br>4. 全局检查 W6 是否存在写操作 |
| **预期结果** | W6 含：周期筛选 / 汇总带 / 五类差异聚合 / 跨单差异列表 / 折叠双账；点击差异跳转到 **`#w4/<单号>/recon`**；页头明示「本工作区只查看与定位，不在此形成正式结果」 |
| **禁止出现的结果** | ① W6 出现「提交」「确认」「发起」类正式动作<br>② W6 出现 C6 正式动作区<br>③ 在 W6 直接修改账目或记录处理意见<br>④ 把 W6 与 L4 对账阶段合并为同一页面 |
| **所属批次** | M5 |

---

## TC-15 · W5 创建、双方确认、非创建人审核生效

| 项 | 内容 |
| --- | --- |
| **对应 R** | R-03 |
| **前置角色** | 陈静（接入配置管理员，**创建人**）、另一接入配置管理员账号、张三（服务商资料负责人）、李强（京东资料核查人员） |
| **前置数据** | 一条新配置草稿，创建人为陈静 |
| **操作步骤** | 1. 陈静创建配置草稿<br>2. 张三确认本地含义<br>3. 李强确认京东接收要求<br>4. 陈静尝试审核生效（创建人身份）<br>5. 换另一接入配置管理员账号审核生效 |
| **预期结果** | 步骤 4：按钮**禁用**，提示「创建人不得审核自己创建或修改的版本」；步骤 5：可执行生效，并记录**配置版本 / 创建人 / 双方确认人 / 审核人 / 审核时间**五要素 |
| **禁止出现的结果** | ① 创建人能审核自己的版本<br>② 双方确认未齐时可生效<br>③ 缺少五要素中的任何一项<br>④ 新增第七个角色来承担审核 |
| **所属批次** | M6 |

---

## TC-16 · CONFIG_ACTIVATE 权限

| 项 | 内容 |
| --- | --- |
| **对应 R** | R-03、R-15 |
| **前置角色** | ① 具备 `CONFIG_ACTIVATE` 的接入配置管理员<br>② 不具备该权限的接入配置管理员<br>③ 系统管理员 |
| **前置数据** | 双方确认齐全、且操作者非创建人的配置 |
| **操作步骤** | 三种角色分别尝试执行「审核生效」 |
| **预期结果** | ① 可用；② **禁用**并提示「需具备配置审核权限（CONFIG_ACTIVATE）」；③ **禁用**并提示系统管理员只负责授予角色与权限 |
| **禁止出现的结果** | ① 无权限账号可生效<br>② 系统管理员可审核生效配置<br>③ 权限判定依赖从哪个页面进入（必须只由角色权限 + 配置状态决定） |
| **所属批次** | M6 |

---

## TC-17 · 重复提交拦截与幂等键

| 项 | 内容 |
| --- | --- |
| **对应 R** | R-13 |
| **前置角色** | 张三（服务商资料负责人） |
| **前置数据** | 工单 `JD202409130001`，资料类型=完工明细，提交版本 v1，已成功提交并收到回执 |
| **操作步骤** | 1. 在相同幂等键下再次触发提交<br>2. 检查 C6 按钮状态<br>3. 检查右栏 R-A1 |
| **预期结果** | C6 因**幂等键重复**（第 4 项前置条件）而禁用；提示指向已有的正式提交版本；**不产生第二条正式记录** |
| **禁止出现的结果** | ① 重复提交成功<br>② R-A1 出现两条并列的 v1 正式记录<br>③ 幂等键被实现为随机数或与「提交版本」无关 |
| **所属批次** | M8 |

---

## TC-18 · 失败后从最近确认节点续办

| 项 | 内容 |
| --- | --- |
| **对应 R** | R-13 |
| **前置角色** | 张三（提交）、王磊（系统管理员，续办） |
| **前置数据** | ① 原系统超时（S6）<br>② 原系统拒绝（S7） |
| **操作步骤** | 1. 构造 S6：提交后原系统超时<br>2. 观察 C6 表现与可用动作<br>3. 构造 S7：原系统返回拒绝<br>4. 观察任务归属与后续动作 |
| **预期结果** | S6：C6 显示「提交未确认」，**不显示成功也不显示失败**，首要动作为「先查询原系统是否已生成正式版本」；确认前右栏**不写入任何内容**；任务转「处理失败」并为系统管理员<br>S7：展示原系统**原文错误信息**（不翻译不改写）+ 错误码 + 时间，动作「退回修改」→ 任务转「待我补充」 |
| **禁止出现的结果** | ① 超时被显示为成功<br>② 超时后右栏出现正式记录<br>③ 拒绝时错误信息被改写或翻译<br>④ 失败后要求从零重做，而非从最近确认节点续办 |
| **所属批次** | M8 |

---

## TC-19 · 1440/1280/1100/900 四档响应式

| 项 | 内容 |
| --- | --- |
| **对应 R** | R-02 |
| **前置角色** | 任意 |
| **前置数据** | 工单 `JD202409130001` 的三栏画布 |
| **操作步骤** | 浏览器宽度依次设为 1440 / 1280 / 1100 / 900，逐档观察三栏 |
| **预期结果** | 1440 三栏全展开（300/flex/320）；1280 左栏 240、右栏 280；1100 左栏折叠为 56px 图标条、右栏 280；900 左栏图标条 + **右栏折叠为顶部摘要条**。**中栏任何一档都不折叠** |
| **禁止出现的结果** | ① 任何一档出现三栏**上下堆叠**<br>② 中栏被折叠或隐藏<br>③ 三状态列在窄屏被隐藏（隐藏顺序只能是：最近更新 → 责任岗位 → **三状态永不隐藏**）<br>④ 出现横向滚动条 |
| **所属批次** | M2 / M9 |

---

## TC-20 · 900px 右栏摘要条与宽抽屉

| 项 | 内容 |
| --- | --- |
| **对应 R** | R-02 |
| **前置角色** | 京东资料核查人员（李强） |
| **前置数据** | 工单 `JD202409130001`：R-A1 已形成、R-A2 尚未形成、R-A3 尚未形成 |
| **操作步骤** | 1. 浏览器宽度设为 900<br>2. 观察顶部摘要条<br>3. 点击展开 |
| **预期结果** | 摘要条显示**三个并列的极简状态**（`资料 ✓v1 ｜ 核查 尚未形成 ｜ 对账 尚未形成`），**只显示「已形成 / 尚未形成」，不显示过程状态与金额**；点击后经**宽抽屉（D2，720–880px）**查看完整三区块 |
| **禁止出现的结果** | ① 摘要条显示「有差异·处理中」或具体金额<br>② 三个区块被合并成一个状态<br>③ 点击使用窄抽屉（420px）<br>④ 点击后跳转到新页面而非抽屉 |
| **所属批次** | M2 / M9 |

---

## TC-21 · 五层记录不覆盖

| 项 | 内容 |
| --- | --- |
| **对应 R** | R-11 |
| **前置角色** | 张三 → 李强 |
| **前置数据** | 工单 `JD202409130001` 已完成 v1 → v2 补充流程 |
| **操作步骤** | 1. 打开 D2「处理过程与版本」<br>2. 检查容器标题与固定提示条<br>3. 逐层核对五层内容与样式<br>4. 检查 AI 草稿层的容器形态 |
| **预期结果** | 抽屉标题为「**处理过程与版本**」，标题下固定提示「**除带正式标识的版本外，其余内容均为过程材料，不构成正式结论。**」；五层各自保留**原有容器样式**（虚线斜纹 / 实线细边 / 主色竖条）；**只有正式版本层带 green 角标**；v1 与 v2 并列 |
| **禁止出现的结果** | ① 抽屉整体被标注为「非正式记录」<br>② 五层被渲染成统一表格行（AI 草稿失去斜纹容器）<br>③ 后一层覆盖前一层<br>④ 多个层级都带 green 正式角标<br>⑤ 未提交草稿与正式结果混排 |
| **所属批次** | M8 / M9 |

---

## TC-22 · R-07 事实核对按需阶段

| 项 | 内容 |
| --- | --- |
| **对应 R** | R-07 |
| **前置角色** | 京东资料核查人员（李强） |
| **前置数据** | ① **工单 A**（`JD202409130001`）：存在**未批准变更**——桥架由 0 米增至 3 米，仅有现场照片、**无书面变更确认**<br>② **工单 B**：无任何变更、无事实冲突，原方案与实际完工一致 |
| **操作步骤** | 1. 打开工单 A 的 L4 画布，观察中栏阶段导航<br>2. 打开工单 B 的 L4 画布，观察中栏阶段导航<br>3. 在工单 A 的「事实核对」阶段中，检查变更结论由谁给出<br>4. 检查该阶段内 AI 输出的容器样式与可用动作 |
| **预期结果** | ① 工单 A：阶段导航中**出现「事实核对」阶段**，且该阶段带未决变更提示<br>② 工单 B：阶段导航中**不出现「事实核对」阶段**（按需出现，非常驻）<br>③ 该阶段内只展示**原方案、现场实际变化、变更原因、影响字段、批准/确认材料**，并明示「变更是否有效仍由原业务系统和原岗位判断」<br>④ AI 输出为**草稿容器**（虚线斜纹 + `AI 草稿` 角标 + 依据入口），动作仅「采纳 / 丢弃」 |
| **禁止出现的结果** | ① **AI 形成正式变更结论**——草稿容器内出现「变更已批准」「变更有效」等结论性表述<br>② AI 草稿被写入右栏 R-A2「正式核查结论」区块<br>③ 平台自建变更审批流（出现「提交变更审批」「变更审批通过」类动作）<br>④ 「事实核对」被设为**常驻固定阶段**（无变更的工单也显示该阶段）<br>⑤ 对未批准变更自动给出「有效」判断，或自动按有效变更推进资料提交 |
| **所属批次** | **M2**（依赖阶段导航；M1 阶段画布仍为六页签结构，本用例不可执行） |

---

## 用例与批次对照

| 批次 | 覆盖用例 | 批次验收门槛 |
| --- | --- | --- |
| M0 | — | 三个原文件哈希不变；页面行为零变化 |
| M1 | TC-01 / TC-02 / TC-03 / TC-04 | 全通过方可进入 M2 |
| M2 | TC-06 / TC-19 / TC-20 / **TC-22** | 全通过方可进入 M3–M6 |
| M3 | TC-11 / TC-12 / TC-13 | 全通过方可进入 M7 |
| M4 | TC-03 / TC-04 / TC-05 | 全通过 |
| M5 | TC-14 | 全通过 |
| M6 | TC-15 / TC-16 | 全通过 |
| M7 | TC-07 / TC-08 / TC-09 | 全通过方可进入 M8 |
| M8 | TC-10 / TC-12 / TC-13 / TC-17 / TC-18 / TC-21 | 全通过方可进入 M9 |
| M9 | 全部 21 条 | 全量回归 |

## R 编号覆盖矩阵

| R | 覆盖用例 |
| --- | --- |
| R-01 | TC-05（间接）、TC-04 |
| R-02 | TC-06、TC-19、TC-20 |
| R-03 | TC-15、TC-16 |
| R-04 | TC-07（间接）、TC-17（间接） |
| R-05 | TC-07 |
| R-06 | TC-05、TC-12 |
| R-07 | **TC-22**（事实核对按需阶段：出现条件、消失条件、AI 不得形成正式变更结论） |
| R-08 | TC-14、TC-12 |
| R-09 | TC-21 |
| R-10 | TC-05 |
| R-11 | TC-07、TC-11、TC-12、TC-13、TC-21 |
| R-12 | TC-07、TC-08、TC-09 |
| R-13 | TC-10、TC-17、TC-18 |
| R-14 | TC-15（新版本不覆盖历史工单） |
| R-15 | TC-01、TC-02、TC-03、TC-04、TC-16 |

> **R-07 已由 TC-22 专项覆盖**（补充于 2026-09-19）。
> v2.1 将「事实核对」定位为**仅在变更或事实冲突时出现**的按需阶段，属条件触发，
> 因此用例不仅断言「何时出现」，同时断言「何时不出现」以及「AI 不得形成正式变更结论」。
