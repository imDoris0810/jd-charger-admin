# 前端迁移计划 v1

> 依据：`AGENTS.md` ｜ `docs/requirements-baseline.md` ｜ `docs/brd.md` ｜ `docs/wireframe-spec-v2.1.docx`（已冻结）
> 现状代码：`docs/prototype.html` / `docs/prototype.css` / `docs/prototype.js`
> 本文件性质：**计划文档**。本轮不修改任何 HTML / CSS / JS

---

## 〇、基线状态与约束

### 0.1 代码现状（冻结时点）

| 文件 | 行数 | SHA-256 |
| --- | --- | --- |
| `docs/prototype.html` | 941 | `ed506fdfabc2d536b8d390b653f18063ea12d324f895d821b3de95482f0856d9` |
| `docs/prototype.css` | 485 | `55356cbd8af83b922be918ec0e17c3e785105a8b2dc254e71841c611fa1c454a` |
| `docs/prototype.js` | 490 | `d72b8b243f0964a4b6d4bc0d3de53f14f1dbef7f9aea3a61c938ac6eb5849eef` |

`git diff -- docs/prototype.*` 输出为空 → 三个文件与上次提交一致，无未提交改动。

### 0.2 本轮硬约束（贯穿全部批次）

1. 不修改 `prototype.html` / `prototype.css` / `prototype.js`（M0 备份除外，且备份为复制而非改写）
2. 不新建替代版前端
3. 不扩展 BRD R-01～R-15 范围
4. 不接入真实 AI 或真实外部接口
5. AI 草稿**永不写入**右栏正式记录
6. 三条状态链**永不合并**

### 0.3 工作量标度

| 标度 | 含义 |
| --- | --- |
| **S** | ≤ 0.5 人日；单点改动，影响面可枚举 |
| **M** | 0.5–1.5 人日；涉及一个页面或一组函数 |
| **L** | ≥ 1.5 人日；跨页面结构性改动，需回归 |

---

## 一、现有代码迁移清单

> 处理方式五选一：**直接复用** ｜ **调整复用** ｜ **拆分迁移** ｜ **推翻重做** ｜ **删除**

### A. 布局与骨架

| # | 当前实现位置 | 当前作用 | v2.1 目标位置 | 处理方式 | 修改原因 | BRD | 量 | 主要风险 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| A1 | `prototype.css:45` `.app` | flex 根容器 | 全局 | 调整复用 | 需容纳三栏常驻 | R-02 | M | 三栏改造波及所有页面 |
| A2 | `prototype.css:46-64` `.sidebar` | 侧边导航容器 | 全局 | 调整复用 | 导航项 4 → 5 | R-01/R-08 | S | 低 |
| A3 | `prototype.css:65-76` `.nav` / `.nav-item` | 导航项与显隐 | 全局 | 调整复用 | 新增 W6 项 | R-15 | S | 低 |
| A4 | `prototype.css:77` `.nav-tag` | 待处理数徽标 | L2 | 直接复用 | — | R-06 | S | 低 |
| A5 | `prototype.css:47-64,81` `.brand` / `.sidebar-foot` | 品牌区与页脚 | 全局 | 调整复用 | 文案改为「京东 × 服务商」 | — | S | 低 |
| A6 | `prototype.css:86` `.main` | 主区容器 | 全局 | 直接复用 | — | — | S | 低 |
| A7 | `prototype.css:87-95` `.topbar` / `.crumb` | 顶栏与面包屑 | 全局 | 调整复用 | 面包屑需细化到**阶段** | R-02 | S | 低 |
| **A8** | `prototype.css:103` `.content` | 内容区 padding 容器 | 全局 | **推翻重做** | 需承担两种形态：L1/L2/L3/W5/W6 的页面容器，与 L4 的三栏 grid 容器 | R-02 | **L** | **高：一处改动影响全部页面** |
| A9 | `prototype.css:96-104` `.user-chip` / `.avatar` / `.role-badge` | 当前用户显示 | 全局 | 直接复用 | — | R-15 | S | 低 |

### B. 角色与权限

| # | 当前实现位置 | 当前作用 | v2.1 目标位置 | 处理方式 | 修改原因 | BRD | 量 | 主要风险 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| B1 | `prototype.css:106-129` + `html:53-59` `.role-switch` 族 | 角色切换下拉 | 全局顶栏 | 直接复用 | **仅原型演示用**，需加注 | R-15 | S | 低 |
| B2 | `prototype.css:132-137` `.perm-bar` | 权限说明条 | 全局 | 直接复用 | — | R-15 | S | 低 |
| **B3** | `prototype.js:7-39` `ROLES` | 六角色定义与权限文案 | 全局 | 调整复用 | 需扩展为**权限矩阵**：左栏可见性、金额遮蔽、正式操作权限、服务商归属 | R-15 | M | **中：权限矩阵全部落在此结构上** |
| B4 | `prototype.js:154,166` `renderRoleMenu` / `toggleRoleMenu` | 角色菜单渲染 | 全局 | 直接复用 | — | R-15 | S | 低 |
| **B5** | `prototype.js:172` `setRole` | 角色应用与页面显隐 | 全局 | 调整复用 | 从「页面级显隐」扩展到「字段级遮蔽 + 动作级禁用」 | R-15 | M | 中 |
| **B6** | — | — | 全局 | **新增** | 权限判定函数（`canSee` / `canAct` / `maskAmount`），供左右栏与 C6 调用 | R-15 | M | 中 |

### C. 工单列表与指标卡

| # | 当前实现位置 | 当前作用 | v2.1 目标位置 | 处理方式 | 修改原因 | BRD | 量 | 主要风险 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| **C1** | `html:78-95` + `css:195-207` `#page-orders` / `.todos` / `.todo` | 四个 KPI 指标卡 | **L1 角色工作台** W1-B 五动作分组入口 | **拆分迁移** | 语义从「指标」变为「入口」；需带动作类型与阻断数，拆为 5 个分组 | R-01/R-06/R-08 | M | 中：卡片语义变更，非纯视觉 |
| C2 | `css:220-233` `.filters` | 筛选条 | L2 / L3 / W6 | 直接复用 | — | R-01 | S | 低 |
| C3 | `css:180-192` `.data` 表格族 | 数据表 | L3 / W6 / L4 / D2 | 直接复用 | — | R-01/R-08/R-09 | S | 低 |
| **C4** | `prototype.js:247,292` `renderOrders` / `filterOrders` | 工单表渲染与筛选 | **L3 工单中心** | 调整复用 | 从首屏降为二级入口；三状态列保持独立 | R-01/R-10 | M | 中 |
| C5 | `prototype.js:281` `countOrders` | KPI 计数 | L1 简报数字 | 调整复用 | 改为**五动作分组计数 + 阻断数** | R-06/R-13 | S | 低 |
| C6 | `html:135` `.note` | 三独立列说明 | L3 | 直接复用 | — | R-10 | S | 低 |

### D. 待办队列

| # | 当前实现位置 | 当前作用 | v2.1 目标位置 | 处理方式 | 修改原因 | BRD | 量 | 主要风险 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| **D1** | `html:142` + `css:347-362` `.task` 族 | 待办事项卡 | **L2 任务队列** | 调整复用 | 字段 8 → **14**；主动作文案改写；**移除「标记不适用」** | R-04/R-06/R-13 | **L** | **高：L2 核心组件** |
| **D2** | `prototype.js:310` `renderTodos` | 待办卡渲染 | L2 | 调整复用 | 五动作分组 + 14 字段 + 就地展开区 | R-06 | **L** | 高 |
| D3 | `prototype.js:303` `setTodoGroup` | 分组切换 | L2 | 调整复用 | 分组维度 2 → **5**（待我确认/补充/解释/等待对方/处理失败） | R-06 | S | 低 |
| **D4** | `css:414-421` `.seg-tabs` | 分段切换器 | L2 分组 / L4 三入口 / L4 阶段导航 | **拆分迁移** | 一处组件服务三种语义，需拆为三个用途并保持视觉一致 | R-04/R-06 | M | 中 |

### E. 工单详情与协同画布

| # | 当前实现位置 | 当前作用 | v2.1 目标位置 | 处理方式 | 修改原因 | BRD | 量 | 主要风险 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| **E1** | `html:371-927` `#page-detail` | 单列卡片流详情页 | **L4 三栏协同画布** | **推翻重做** | 单列 → 三栏常驻；页头改为「标识 + 三状态 + 不变式」 | R-02 | **L** | **高：改动量最大** |
| E2 | `css:240-256` + `html:398` `.statusbar` / `.status-seg` | 三段式独立状态条 | L4 左栏 L2 | 调整复用 | 从页头移入左栏并压缩 | R-10 | S | 低 |
| E3 | `css:258-264` `.invariant` / `.neq` | 三个状态不等式 | L4 左栏状态条下方 | 直接复用 | — | R-10 | S | 低 |
| **E4** | `html:425` + `css:168-177` `#detail-tabs` / `.tabs` / `.tab` | 六个页签 | L4 中栏**阶段导航** | 调整复用 | 六页签 → 4 阶段；视觉弱化，**不再切换左右栏** | R-02 | M | 中 |
| E5 | `css:466-467` `.tabpane` / `.tabpane.active` | 页签面板显隐 | L4 中栏阶段面板 | 调整复用 | — | R-02 | S | 低 |
| E6 | `prototype.js:385` `showTab` | 页签切换 | L4 阶段导航 | 调整复用 | 六 → 四阶段；深链接改 `#w4/<单号>/<阶段>` | R-02 | M | 中 |
| E7 | `prototype.js:393` `setPath` | 三资料入口切换 | L4 资料交接阶段 | 直接复用 | — | R-04 | S | 低 |
| **E8** | `html:435-467` T1 工单概览 | 概览全部字段 | L4 左栏摘要 + **D2 完整事实** | **拆分迁移** | 摘要常驻左栏，全量进宽抽屉 | R-01/R-02 | M | 中 |
| **E9** | `html:468-503` T2 方案与变更 | 变更事实追溯 | L4 左栏折叠分组 + 中栏**「事实核对」按需阶段** | **拆分迁移** | 仅在变更或事实冲突时升格为阶段，不常驻 | R-07 | M | 中 |
| E10 | `html:504-635` T3 资料与交接 | 记录层级与三入口 | L4 中栏**资料交接阶段**（AI 整理台） | 调整复用 | 保持三入口与五层记录结构 | R-04/R-05/R-11 | M | 中 |
| **E11** | `html:636-761` T4 核查复核 | 字段清单式核查 | L4 中栏**核查阶段（问题优先）** | **推翻重做** | 信息次序从「逐字段」改为「问题在前，字段在后」 | R-06 | **L** | **高：信息结构反转** |
| E12 | `html:762-862` T5 业务对账 | 单工单双账对照 | L4 中栏**对账阶段（差异优先）** | 调整复用 | 完整双账折叠为原始依据，差异对象前置 | R-08 | M | 中 |
| **E13** | `html:863-927` T6 档案与历史 | 档案 + 时间线 | **D2 处理过程与版本** | **拆分迁移** | 从页签移入宽抽屉；v1/v2 并列 | R-09/R-11 | M | 中 |
| **E14** | `css:266-283` `.layer` 三态容器 | 草稿/确认稿/正式容器 | L4 中栏 C4/C5 + D2 | **直接复用** | **v2.1 的地基，最重要的可复用资产** | R-11/R-12 | S | 低 |
| E15 | `css:285-290` `.evidence-line` | 依据摘要行 | 所有 AI 草稿容器 | **直接复用** | — | R-12 | S | 低 |
| **E16** | `css:292-298` `.gaps` / `.gap.missing` / `.gap.question` | 缺失证据与待确认问题 | AI 草稿容器底部 | **直接复用** | **不可折叠**，是基线硬要求 | R-12 | S | 低 |
| E17 | `css:299-302` `.field-unconfirmed` | 字段级未确认圆点 | 中栏各字段 | 直接复用 | — | R-11 | S | 低 |
| E18 | `css:423-431` `.flow` / `.step` 族 | 流程条（auto/ai/ext） | 三资料入口 | 直接复用 | — | R-04 | S | 低 |
| E19 | `css:437-439` `.kv` | 定义列表 | 全局 | 直接复用 | — | — | S | 低 |
| E20 | `css:433,441-450` `.sect` / `.note` / `.readonly-flag` / `.empty` | 排版辅助 | 全局 | 直接复用 | — | — | S | 低 |
| **E21** | — | — | L4 中栏 **C6 当前阶段正式动作区** | **新增** | 三阶段变体 + 只读同步态；C6 四项提交前置条件 | R-04/R-06/R-08/R-13 | **L** | **高：唯一正式记录入口** |
| **E22** | — | — | L4 右栏 **R-A1/R-A2/R-A3 正式记录栈** | **新增** | 四项准入信息 + 严格空态 | R-11 | **L** | **高：准入判定写错即违规** |
| E23 | — | — | L4 右栏 **R-B 追溯入口组** | 新增 | 只放入口不放内容 | R-09 | S | 中 |

### F. 抽屉

| # | 当前实现位置 | 当前作用 | v2.1 目标位置 | 处理方式 | 修改原因 | BRD | 量 | 主要风险 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| F1 | `css:367-396` + `html:928` `.drawer` / `.drawer-backdrop` | 单一 470px 抽屉 | **D1/D2 同组件两尺寸** | 调整复用 | 新增 `size: lg`（720–880）与 4 种视图 | R-12/R-09 | M | 中 |
| F2 | `css:388-396` `.drawer-head` / `.drawer-close` | 抽屉头部 | D1/D2 | 直接复用 | — | — | S | 低 |
| F3 | `css:398-407` `.evidence-item` 族 | 依据四要素条目 | D1 | 直接复用 | — | R-12 | S | 低 |
| F4 | `prototype.js:432,465` `openDrawer` / `closeDrawer` | 抽屉控制 | D1/D2 | 调整复用 | 增加尺寸参数与视图类型 | R-09/R-12 | M | 中 |
| F5 | `prototype.js:405` `EVIDENCE` | 依据数据 | D1 | 调整复用 | 补齐四要素结构（来源/文件/位置/摘录） | R-12 | S | 低 |

### G. 时间线

| # | 当前实现位置 | 当前作用 | v2.1 目标位置 | 处理方式 | 修改原因 | BRD | 量 | 主要风险 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| G1 | `css:409-422` `.timeline` / `.tl-item` | 时间线 | D2 + L4 左栏 | **直接复用** | 已是「时间线 + 证据链」形态 | R-09 | S | 低 |
| G2 | `prototype.js:423` `TIMELINE` | 时间线数据 | D2 | 调整复用 | 增加版本层标记（v1/v2） | R-11 | S | 低 |

### H. 业务对账

| # | 当前实现位置 | 当前作用 | v2.1 目标位置 | 处理方式 | 修改原因 | BRD | 量 | 主要风险 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| H1 | `css:425-434` `.dual` / `.col-head` | 双栏对照 | L4 对账阶段折叠区 + W6-E | 调整复用 | 从主视图降为「可展开的原始依据」 | R-08 | M | 中 |
| H2 | `css:436-448` `.mapping` / `.map-row` | 项目对应关系 | L4 对账差异优先视图 | 调整复用 | 从「对应表」改为「差异对象卡」基础 | R-08 | M | 中 |
| **H3** | `html:179-239` + `prototype.js:368` `#page-recon` / `renderRecon` | 对账批次表 | **拆为 W6 业务对账工作区 + L4 对账阶段** | **拆分迁移** | W6 负责跨单批量，L4 负责单工单；**不可互相替代** | R-08 | **L** | **高：边界最易混淆** |
| H4 | `prototype.js:144` `RECON` | 对账数据 | W6 | 调整复用 | 增加周期/批次维度与五类差异分类 | R-08 | M | 中 |

### I. 接入配置

| # | 当前实现位置 | 当前作用 | v2.1 目标位置 | 处理方式 | 修改原因 | BRD | 量 | 主要风险 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| **I1** | `html:241-370` `#page-config` | 配置列表 + 详情 | **W5 接入配置工作区** | 调整复用 | 增加审核人资格判定与审核记录 | R-03 | **L** | 高 |
| I2 | `css:459-468` + `html:331` `.signoff` / `.sign-slot` | 双方签章位 | W5-E | 调整复用 | 增加创建人 / 审核人 / 时间字段 | R-03 | M | 中 |
| **I3** | — | — | W5-E 治理逻辑 | **新增** | 同角色双人分权：非创建人 + `CONFIG_ACTIVATE` | R-03 | M | 中 |

### J. 路由与数据

| # | 当前实现位置 | 当前作用 | v2.1 目标位置 | 处理方式 | 修改原因 | BRD | 量 | 主要风险 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| J1 | `prototype.js:210,218` `PAGE_META` / `go` | 页面路由 | 全局 | 调整复用 | 四项 → 五项；画布路由分层 | R-15 | M | 中 |
| J2 | `prototype.js:233` `hashchange` | 深链接 | 全局 | 调整复用 | 支持 `#w4/<单号>/<阶段>` 与 `#w6?period=` | R-02 | M | 中 |
| J3 | `prototype.js:475-480` 启动参数 | 初始页/角色参数 | 全局 | **直接复用** | — | — | S | 低 |
| J4 | `prototype.js:46` `ORDERS` | 工单数据 | 全局 | 调整复用 | 增加服务商组织字段、三类状态分离 | R-01/R-10 | M | 中 |
| J5 | `prototype.js:98` `TODOS` | 待办数据 | L2 | 调整复用 | 增加 `actionType` / `blocking` / 责任角色细化 | R-06 | M | 中 |
| J6 | `prototype.js:243` `badge()` | 状态徽标渲染 | 全局 | **直接复用** | — | R-10 | S | 低 |
| **J7** | — | — | L4 C6 | **新增** | 幂等键与提交状态模型（未提交/提交中/超时/拒绝/已回执） | R-13 | M | 中 |

### K. 响应式

| # | 当前实现位置 | 当前作用 | v2.1 目标位置 | 处理方式 | 修改原因 | BRD | 量 | 主要风险 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| **K1** | `prototype.css:479` `@media (max-width:1100px)` | 单断点响应式 | 四档断点（1440/1280/1100/900） | **推翻重做** | 现有规则把三栏改为**上下堆叠**，v2.1 明令禁止；且只有一档 | R-02 | **L** | **高：违规风险** |

### 汇总

| 处理方式 | 条目数 | 占比 |
| --- | --- | --- |
| 直接复用 | 20 | 39% |
| 调整复用 | 19 | 37% |
| 拆分迁移 | 5 | 10% |
| 推翻重做 | 4 | 8% |
| 新增 | 5 | — |
| 删除 | 1（T4 字段清单式布局被问题优先取代） | 2% |
| **合计** | **51 + 5 新增** | |

---

## 二、重点模块处理说明

| 模块 | 处理方式 | 说明 |
| --- | --- | --- |
| **原侧边导航** | 调整复用 | 保留 224px 深色骨架与 `.nav-item`；导航项 4 → 5，新增 W6；`服务商接入配置` 保持独立入口。品牌副标题由「京东 · 内网工具」改为「京东 × 服务商」 |
| **原工单列表与指标卡** | 拆分迁移 | 工单表整体迁入 L3（三状态独立列不动）；四个 KPI 卡**拆为 L1 的五个动作分组入口**，语义从"看数字"变为"点进去干活" |
| **原角色切换** | 直接复用 + 扩展 | 组件本身原样保留（并标注仅供原型演示）；`ROLES` 数据结构扩展为权限矩阵载体，`setRole` 从页面级显隐扩展到字段遮蔽与动作禁用 |
| **原 T1～T6 页签** | 拆分迁移 | T1 → 左栏摘要 + D2；T2 → 左栏分组 + 按需阶段；T3 → 资料交接阶段；T4 → 核查阶段（**推翻重做**为问题优先）；T5 → 对账阶段；T6 → D2。**六个页签降级为 4 个阶段 + 2 个常驻栏位** |
| **`.content`** | 推翻重做 | 必须同时支持「页面容器」与「三栏 grid 容器」两种形态。这是本次改造中影响面最大的单点 |
| **`.tabs` / `.tabpane`** | 调整复用 | 显隐机制保留，视觉弱化（去掉强对比 count 胶囊），语义从"页签"改为"阶段" |
| **`.layer`** | 直接复用 | 三态容器（draft/confirmed/official）是 v2.1 的地基，一行不改 |
| **`.gaps`** | 直接复用 | 缺失证据（红）/ 待确认问题（黄）结构完全符合基线 §9.2，且不可折叠的要求已满足 |
| **`.evidence-line`** | 直接复用 | 依据摘要行 + 展开入口，直接对应 D1 |
| **`.timeline`** | 直接复用 | 已是「时间线 + 证据链」，迁入 D2 后增加 v1/v2 并列标记 |
| **`.dual`** | 调整复用 | 从对账主视图**降级**为折叠的原始依据区 |
| **现有 hash 路由** | 调整复用 | 保留 `#l1/#l2/#l3/#w4/#w5` 与 `?role=` 机制；新增 `#w6`、`#w4/<单号>/<阶段>`、`#w6?period=` |
| **现有模拟数据** | 调整复用 | `ORDERS` / `TODOS` / `RECON` 结构与基线一致，扩展字段即可；不重造数据 |
| **现有响应式规则** | 推翻重做 | 单断点且会**上下堆叠三栏**，与 v2.1「禁止堆叠、左右栏折叠为条或浮层」直接冲突 |

---

## 三、开发批次计划

> 原则：**每批次必须可独立验证、可独立回退**。不允许一次性重写全部原型。

### M0 · 代码备份、基线锁定与数据模型

| 项 | 内容 |
| --- | --- |
| **修改文件** | 新建 `docs/prototype-v2.0-backup/`（复制三个文件）；新建 `docs/prototype.data.js`（数据模型），`prototype.js` 仅追加引用行 |
| **实现内容** | 冻结备份；定义 `ROLES`（含权限矩阵字段）、`ORDERS`（三类状态分离）、`TODOS`（五动作类型 + blocking）、`RECON`（周期/批次 + 五类差异）、`CONFIG`（创建人/审核人）、`AUDIT`（审核记录） |
| **可复用旧代码** | 现有 `ROLES` / `ORDERS` / `TODOS` / `RECON` 全部数据（扩展字段，不重写） |
| **不允许触碰** | `prototype.css`；任何页面的渲染函数 |
| **完成标准** | 三个原文件 SHA-256 与 §0.1 一致；备份目录存在且内容一致；新数据文件可被 `prototype.js` 引用后页面**行为与改造前完全一致** |
| **测试方法** | 逐页截图对比 M0 前后；`shasum` 校验 |
| **失败回退** | 删除备份目录与 `prototype.data.js`，还原 `prototype.js` 一行引用 |
| **对应 R** | R-15（角色数据）、R-01（工单数据）、R-10（状态分离）、R-13（幂等字段） |

### M1 · 五项一级导航、路由与六角色切换

| 项 | 内容 |
| --- | --- |
| **修改文件** | `prototype.html`（导航区）、`prototype.js`（`PAGE_META`/`go`/`setRole`）、`prototype.data.js` |
| **实现内容** | 侧边栏 4 → 5 项（新增 W6）；品牌文案；路由表扩展（`#w6`、阶段路由）；`ROLE_PARAM` 保留 |
| **可复用旧代码** | `.nav` / `.nav-item` / `.nav-tag` 全部 CSS；`renderRoleMenu` / `toggleRoleMenu` 原样；`hashchange` 机制 |
| **不允许触碰** | `.content` 布局、`.tabs`、任何 `.layer` 容器 |
| **完成标准** | 五项导航可切换；`#w6` 可深链接；六角色切换后可见导航项正确；W6 页面暂为占位符 |
| **测试方法** | TC-01、TC-02、TC-03、TC-04、TC-20 |
| **失败回退** | 导航区 HTML 与路由表为独立区块，`git checkout` 该区块即可 |
| **对应 R** | R-15、R-01、R-08 |

### M2 · L4 三栏画布骨架、阶段导航与响应式折叠

| 项 | 内容 |
| --- | --- |
| **修改文件** | `prototype.css`（`.content` 重构 + 四档断点）、`prototype.html`（`#page-detail` 重构） |
| **实现内容** | `.content` 双形态；三栏 grid；左栏（摘要 + 三状态条 + 事实分组 + 展开入口）；中栏（面包屑 + 任务卡 + 阶段导航 + 占位区）；右栏（占位）；四档折叠规则 |
| **可复用旧代码** | `.statusbar` / `.invariant` / `.card` / `.kv` / `.sect` 原样；`.tabs` 改造为阶段导航；`showTab` 改造 |
| **不允许触碰** | 中栏 C4/C5/C6 内部结构（本批次只放占位）；右栏内容；`.layer` |
| **完成标准** | 1440/1280/1100/900 四档下三栏均不上下堆叠；左栏事实摘要有数据；阶段导航可切换且**不影响左右栏** |
| **测试方法** | TC-05、TC-06、TC-18、TC-19 |
| **失败回退** | `.content` 新规则置于独立 CSS 区块并保留旧规则注释；`#page-detail` 旧结构保留在备份 |
| **对应 R** | R-02、R-10 |

### M3 · 右栏 R-A1/R-A2/R-A3 正式记录栈与版本容器

| 项 | 内容 |
| --- | --- |
| **修改文件** | `prototype.html`（右栏）、`prototype.css`（R-A 区块样式）、`prototype.js`（准入判定函数） |
| **实现内容** | 三区块独立渲染；**四项准入信息**判定；严格空态（仅「尚未形成正式记录」+「前往当前阶段」）；版本并列容器 |
| **可复用旧代码** | `.layer.official` 样式；`.badge-green`；`.card` |
| **不允许触碰** | C4 AI 草稿容器、C5 人工确认区（不得让它们写入右栏） |
| **完成标准** | 无正式结果时三区块只显示规定文案，**不出现任何过程状态或金额**；有正式结果时显示回执号/操作人/时间 |
| **测试方法** | TC-10、TC-11、TC-12、TC-20 |
| **失败回退** | 右栏为独立容器，删除该区块即可回到 M2 状态 |
| **对应 R** | R-11、R-13 |

### M4 · L1 角色工作台、L2 任务队列、L3 工单中心

| 项 | 内容 |
| --- | --- |
| **修改文件** | `prototype.html`（新增 L1 页；L2/L3 迁入）、`prototype.js`（`renderOrders` / `renderTodos` / `countOrders` / `setTodoGroup` 改造）、`prototype.css`（`.task` 扩展） |
| **实现内容** | L1 简报带（程序模板）+ 五分组入口 + 优先处理 + 我负责的工单；L2 五动作分组 + 14 字段卡 + 就地展开；L3 三状态独立列 |
| **可复用旧代码** | `.data` / `.filters` / `.badge` 原样；`renderOrders` / `filterOrders` 主体；`.todo` 卡样式拆用；`.task` 基础结构 |
| **不允许触碰** | 右栏 R-A；C6；`.layer` |
| **完成标准** | 简报句全部由模板填充（无 LLM）；五分组计数正确；L2 卡片 14 字段齐全且**无「标记不适用」**；L3 保留三个独立状态列 |
| **测试方法** | TC-04、TC-05、TC-13、TC-21 |
| **失败回退** | L1 为新增页面可整体移除；L2/L3 改造集中在三个渲染函数，逐个回退 |
| **对应 R** | R-01、R-06、R-08、R-10、R-13 |

### M5 · W6 业务对账工作区与 L4 单工单跳转

| 项 | 内容 |
| --- | --- |
| **修改文件** | `prototype.html`（`#w6` 页）、`prototype.js`（`renderRecon` 拆分）、`prototype.data.js` |
| **实现内容** | W6-A 周期筛选 / B 汇总带 / C 五类差异聚合 / D 跨单差异列表 / E 折叠双账；点击差异 → `#w4/<单号>/recon` |
| **可复用旧代码** | `.data` / `.filters` / `.dual` / `.mapping` |
| **不允许触碰** | W6 **不得**出现任何正式动作入口；不得新发起 AI 调用 |
| **完成标准** | W6 全页无写操作；点击差异正确跳转到 L4 对账阶段；W6 与 L4 数据一致 |
| **测试方法** | TC-14 |
| **失败回退** | `#w6` 为独立页面，移除导航项即可回退 |
| **对应 R** | R-08 |

### M6 · W5 接入配置治理与双人分权

| 项 | 内容 |
| --- | --- |
| **修改文件** | `prototype.html`（`#page-config` 调整）、`prototype.js`（审核资格判定）、`prototype.data.js` |
| **实现内容** | 六项治理动作角色归属；双方签章位；**非创建人 + `CONFIG_ACTIVATE`** 判定；审核记录五要素；版本历史 |
| **可复用旧代码** | `#page-config` 全部；`.signoff` / `.sign-slot` |
| **不允许触碰** | 不新增第七个角色；系统管理员不得审核生效 |
| **完成标准** | 创建人账号下「审核生效」禁用；无 `CONFIG_ACTIVATE` 账号下禁用；双方签章未齐时禁用；三种禁用各有独立提示文案 |
| **测试方法** | TC-15、TC-16、TC-17 |
| **失败回退** | 审核逻辑集中在单一判定函数，回退该函数即可 |
| **对应 R** | R-03、R-14、R-15 |

### M7 · 四种 AI 辅助形态及人工降级表单

| 项 | 内容 |
| --- | --- |
| **修改文件** | `prototype.html`（C4 各形态）、`prototype.css`（降级表单）、`prototype.js`（形态切换 + 降级） |
| **实现内容** | 整理台 / 映射工作室 / 问题优先 / 差异优先；四种降级表单；降级提示条 |
| **可复用旧代码** | `.layer.draft` / `.evidence-line` / `.gaps` / `.flow.step.ai` / `.field-unconfirmed` 全部原样 |
| **不允许触碰** | **不接真实 AI**；草稿永不进右栏；`.gaps` 不可折叠 |
| **完成标准** | 四种形态视觉互不雷同；AI 失败时降级为**可完成同样工作的表单**而非错误提示 |
| **测试方法** | TC-07、TC-08、TC-09 |
| **失败回退** | 四形态为 C4 内独立分支，逐个禁用 |
| **对应 R** | R-05、R-06、R-12 |

### M8 · 端到端流程、异常续办与幂等演示

| 项 | 内容 |
| --- | --- |
| **修改文件** | `prototype.js`（C6 状态机 + 幂等键）、`prototype.data.js` |
| **实现内容** | C6 三阶段变体 + 只读同步态；四项提交前置条件；幂等键 `京东工单号＋资料类型＋提交版本`；S6 超时 / S7 拒绝；从最近确认节点续办 |
| **可复用旧代码** | 无（全新增） |
| **不允许触碰** | 失败或处理中**不得**形成正式结果；不得重复生成正式记录 |
| **完成标准** | 重复提交被拦；超时后提示先查询原系统；v1/v2 全链路并列保留 |
| **测试方法** | TC-16、TC-17、TC-12 |
| **失败回退** | C6 状态机为独立模块 |
| **对应 R** | R-13、R-04、R-06、R-08 |

### M9 · 全量角色、响应式与视觉回归

| 项 | 内容 |
| --- | --- |
| **修改文件** | 三个文件（只做缺陷修复，不加功能） |
| **实现内容** | 六角色 × 五页面 × 四档断点全矩阵回归；视觉一致性；空态/加载/异常/降级四态全覆盖 |
| **可复用旧代码** | — |
| **不允许触碰** | 不新增业务范围 |
| **完成标准** | 全部 TC 用例通过；无空白页、无布局溢出、无未遮蔽字段 |
| **测试方法** | 全量 TC + 截图对比 |
| **失败回退** | 按批次回退 |
| **对应 R** | R-01～R-15 全部 |

### 批次依赖

```
M0 ─▶ M1 ─▶ M2 ─┬─▶ M3 ─▶ M7 ─┐
                ├─▶ M4 ────────┤
                ├─▶ M5 ────────┼─▶ M8 ─▶ M9
                └─▶ M6 ────────┘
```

M3/M4/M5/M6 可并行，但均依赖 M2 的三栏骨架。

---

## 四、风险登记册

| # | 风险 | 等级 | 触发点 | 缓解 |
| --- | --- | --- | --- | --- |
| 1 | `.content` 重构波及全部页面 | 高 | M2 | 新规则独立区块；保留旧规则可一键回切；逐页截图对比 |
| 2 | 三栏在窄屏被改为上下堆叠（违反 v2.1） | 高 | M2/M9 | 四档断点逐档验证；TC-18 明确禁止堆叠 |
| 3 | AI 草稿被写入右栏 | 高 | M3/M7 | 右栏准入四项 + 独立判定函数；TC-11 双向断言 |
| 4 | W6 与 L4 对账边界混淆 | 中 | M5 | W6 不予任何写操作；TC-14 |
| 5 | 队列层文案被误读为已形成正式结论 | 中 | M4 | 统一为「确认本项处理结果」；TC-05 |
| 6 | 编号/状态在多处重复实现导致不一致 | 中 | M4/M8 | 状态计算集中在数据层 |

---

## 五、待裁决问题（编码前）

见主回复第五节。
