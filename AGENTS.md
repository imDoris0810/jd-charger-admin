# AGENTS.md — 项目宪法

> 本文件是本项目的最高准则。所有 AI 助手与开发者在任何操作前必须先阅读并遵守本文约定。如其它文档与本文冲突，以本文为准。

## 1. 项目简介

**京东充电桩安装费用核对平台**

- **使用方**：京东业务侧，约 20 人，仅限内网使用。
- **核心流程**：
  1. 服务商通过工单链接提交费用明细（无需登录、无账号）。
  2. 系统自动将提交的明细与京东标准库进行核对。
  3. 核对通过后生成结算账单，供业务侧确认与结算。

## 2. 用户角色

| 角色 | 是否有账号 | 职责 |
| --- | --- | --- |
| **admin** | 有 | 维护标准库（充电桩安装费用标准） |
| **operator** | 有 | 核对费用明细、处理结算 |
| **服务商** | 无账号 | 通过工单链接提交费用明细 |

## 3. 技术栈（锁定，不可变更）

| 层 | 技术 |
| --- | --- |
| 前端框架 | Vite + React 18 |
| 语言 | TypeScript |
| 样式 | Tailwind CSS v3 |
| 路由 | React Router v6 |
| 前端托管 | Cloudflare Pages |
| 数据库 | Cloudflare D1 |
| 后端 | Cloudflare Workers |

> 以上技术栈已锁定，未经确认不得更换或引入替代方案。

## 4. 目录结构约定

```
src/features/               # 按功能模块划分
  orders/                   # 工单管理
  reconciliation/           # 费用核对
  settlement/               # 结算账单
  dashboard/                # 数据看板
  fee-library/              # 标准费用库维护
  submit/                   # 服务商提交（无账号，走 token 免登录）
src/shared/                 # 公共组件与工具
functions/api/              # Cloudflare Workers 后端
memory-bank/                # 项目记忆文件夹
docs/                       # 需求文档
```

- 新功能一律放入 `src/features/` 下对应的模块目录。
- 跨模块复用的组件与工具放入 `src/shared/`。
- 后端逻辑统一放在 `functions/api/`。

## 5. 禁区（绝对禁止）

以下操作在任何情况下都**不得**执行：

1. **不得修改 `src/config/schema.ts`** —— 该文件是标准核对的核心数据结构，改动需另行单独确认。
2. **不得安装未确认的 UI 库** —— 引入任何 UI 组件库前必须先征得确认。
3. **不得读写 C 端数据库** —— 只允许访问本平台（内网业务侧）的数据库，严禁触及 C 端（消费者端）数据。

## 6. 工作约定

- 提交前先核对是否触及以上禁区。
- 涉及 `src/config/schema.ts`、UI 库选型、数据库访问范围的改动，必须先向用户确认。
- 需求文档写入 `docs/`，项目记忆与进度写入 `memory-bank/`。
