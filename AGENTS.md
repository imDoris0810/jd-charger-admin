# AGENTS.md — 项目宪法

> 本文件是本项目的工程与协作准则。所有 AI 助手与开发者在任何操作前必须先阅读并遵守本文约定。
>
> **上位依据**：需求与产品边界以 [docs/requirements-baseline.md](docs/requirements-baseline.md)（最终需求基线）为准。本文件与需求基线冲突时，以需求基线为准，并回头修订本文件。
>
> **版本状态**：1.0 已用 `git tag v1.0-baseline` 冻结。本文件对应新版（协同管理后台），1.0 模块的处置见需求基线 §14。

---

## 1. 项目定位

**京东充电桩安装协同管理后台**

在不替换服务商本地系统、京东安装履约系统、京东安装管理系统及京东账单系统的前提下，新增一个围绕**同一安装工单**的协同管理后台，通过可配置的资料交接、确定性字段转换、辅助核查、双账对比和全程追溯，减少人工改表、重复录入、跨系统查找及差异解释成本。

**产品本质**：本产品是连接「服务商本地系统」和「京东原业务系统」的**协同中间层**，不是新的完整安装履约系统。

### 1.1 平台负责

- 聚合同一安装工单在不同系统中的关联信息；
- 配置服务商本地记录与京东标准字段、项目和办理位置的对应关系；
- 支持本地自动交接、文件交付、直接使用京东入口三种资料路径；
- 使用普通程序完成确定性转换、校验、计算、提交和结果返回；
- 使用结构化 Prompt 生成字段映射建议、资料整理草稿、核查提示和差异解释；
- 保留原始记录、配置版本、草稿、正式版本、问题、回复和处理历史；
- 对服务商本地应收与京东正式账单进行独立对照；
- 在系统或自动能力异常时，从原办理位置继续处理。

### 1.2 平台不负责

- 重新建设勘测、排班、施工、库存、领退料、验收和客户交付系统；
- 重新建设京东正式账单或付款系统；
- 覆盖服务商本地成本、工资、其他客户和经营数据；
- 代替原岗位完成专业判断、审批、正式核查或责任认定；
- 自动修改服务商应收、京东账单或执行付款；
- 开发独立 Skill、复杂 Agent、RAG 或向量数据库。

---

## 2. 最高业务原则

1. **原系统继续负责正式业务。** 新后台只聚合、交接、辅助、对照和追溯。
2. **原始数据不得覆盖。** 原始记录、转换结果、AI 草稿、人工修改和正式结果分层保存。
3. **同一事实只确认一次。** 已在原责任位置确认且成功交接的内容不得要求重复录入。
4. **不同事实由不同系统负责。** 不设置笼统的「唯一真相系统」，按字段和记录类型确定权威来源。
5. **三类状态独立。** 现场履约、资料交接、业务对账不能合并成一个总状态。
6. **双账独立。** 服务商本地应收和京东正式账单互不覆盖、互不复制生成。
7. **AI 只提供草稿。** 无依据时必须输出「证据不足」，不得编造事实或自动下正式结论。
8. **正式结果可追溯。** 必须知道来源、配置版本、提交版本、确认人、办理位置和历史变化。
9. **异常可以续办。** 新入口或自动能力不可用时，原入口和原业务能力仍可继续使用。
10. **分批迁移。** 按服务商、资料类型、办理事项和模板版本逐步启用。

---

## 3. 系统边界与记录归属

| 系统/位置 | 继续负责的内容 | 新后台的关系 |
| --- | --- | --- |
| 服务商本地系统 | 本地任务、人员安排、勘测、施工、完工、材料领退、库存、本地应收 | 读取经服务商确认且在接入范围内的记录；返回交接结果和问题 |
| 京东订单系统 | 购买范围、安装服务和工单来源 | 读取订单与服务范围，建立工单关系 |
| 京东安装履约系统 | 接单、预约、派工、条件、方案、变更、施工、验收和用户确认 | 引用正式方案、变更、履约和验收结果；不重建流程 |
| 京东安装管理系统 | 正式资料接收、校验、补充、复核、历史和正式核查结论 | 调用原校验/提交能力，读取正式版本及结果 |
| 京东账单模块 | 京东正式账单、调整和结算结果 | **只读**正式账单并用于对账，不修改、不付款 |
| 新协同后台 | 接入配置、交接过程、草稿、问题、回复、对应关系、差异和追溯 | 保存新增的过程记录，**不冒充原系统正式记录** |

不同字段分别指定权威来源。出现不一致时，**不自动选择一方覆盖另一方**；平台展示差异、依据和责任岗位，由原责任岗位处理。

---

## 4. 用户角色与访问方式

| 角色 | 主要职责 | 不允许的行为 |
| --- | --- | --- |
| 服务商资料负责人 | 确认本地交接范围、上传文件、提交资料、查看退回问题、补充材料 | 不得确认京东正式核查结论 |
| 服务商业务负责人 | 确认本地应收、查看差异、提供合同或批准依据、反馈处理意见 | 不得修改京东账单 |
| 京东资料核查人员 | 查看正式资料、提出问题、复核补充、形成原系统正式结论 | 不得替服务商修改本地事实 |
| 京东业务对账人员 | 建立或确认账目对应、查看差额、记录处理意见 | 不得直接改账或执行付款 |
| 接入配置管理员 | 维护接入配置草稿、适用范围和版本 | 不得单方面让配置生效 |
| 系统管理员 | 用户、角色、权限和基础维护 | 不得代替业务岗位作专业结论 |

### 4.1 访问方式

- **京东内部四角色**（京东资料核查人员、京东业务对账人员、接入配置管理员、系统管理员）：仅限内网访问。
- **服务商侧两角色**（服务商资料负责人、服务商业务负责人）：**公网访问 + 账号鉴权**，按角色区分权限。
- **不再使用** 1.0 的「token 免登录提交链接」模式。
- 现场施工人员继续使用服务商本地系统。MVP 不建设现场工人端、移动端、照片直传、排班或施工进度填报能力。

---

## 5. 核心流程（MVP 必须跑通的链路）

```text
建立接入配置并审核生效
→ 从本地记录、文件或京东入口取得资料
→ 普通程序转换与校验
→ AI 整理非结构化内容（可选）
→ 人工确认
→ 调用原系统正式提交
→ 返回结果或退回问题
→ 补充与复核
→ 读取两边独立账目
→ 程序对应项目并计算差额
→ AI 生成差异解释草稿（可选）
→ 责任人处理
→ 保存完整追溯记录
```

### 5.1 三种资料入口

| 路径 | 链路 |
| --- | --- |
| **A. 本地系统自动交接** | 服务商在本地确认 → 新后台读取授权内容 → 按生效配置转换和校验 → 调用京东原校验/提交能力 → 正式结果返回本地任务 |
| **B. 文件交付** | 服务商上传本次文件并确认交付 → 新后台转换和校验 → 人工确认后正式提交 → 结果返回文件交付事项 |
| **C. 直接使用京东入口** | 服务商在京东入口填写/导入 → 调用原标准校验和提交能力 → 结果返回当前京东工单 |

三种路径**使用同一套正式校验和提交能力**。没有原核查要求的资料，不得因为接入新后台而新增审批。

---

## 6. 状态模型

### 6.1 三个独立业务状态

| 状态维度 | 建议状态 |
| --- | --- |
| 现场履约状态 | 待履约 / 履约中 / 待整改 / 已完工 / 已验收 |
| 资料交接状态 | 未交接 / 转换中 / 待确认 / 提交中 / 待补充 / 已完成 / 异常 |
| 对账处理状态 | 未开始 / 待对账 / 有差异 / 处理中 / 已确认 |

**不得合并三个状态。** 且：

- 「资料已收到」≠「正式提交成功」
- 「正式提交成功」≠「核查通过」
- 「对账已确认」≠「已经付款」

### 6.2 记录层级

```text
原始记录 → 转换结果 → AI草稿 → 人工确认稿 → 正式提交版本 → 原系统正式结论
```

**后一个层级不能覆盖前一个层级。** 未提交草稿和未解决问题必须与正式结果分开显示。

---

## 7. 普通程序与 AI 的分工

### 7.1 普通程序必须负责

配置读取和版本判断、字段转换和单位换算、项目合并和金额计算、必填/格式/范围/重复校验、工单资料账目关联、正式提交和结果返回、状态流转、幂等/防重复/重试、权限和操作日志。

**这些是确定性能力，不得交给 AI 判断。**

### 7.2 结构化 Prompt 只负责四件事

1. 字段映射建议；
2. 非结构化资料整理；
3. 核查问题辅助；
4. 对账差异解释。

### 7.3 技术实现

不开发独立 Skill。使用**一个通用 AI 辅助接口 + 四个 Prompt 模板**：

```text
POST /api/ai/assist

scenario:
- mapping_suggestion
- material_organizing
- verification_assistance
- reconciliation_explanation
```

输出采用固定 JSON Schema，至少包含：摘要、建议、依据、缺失证据、待人工确认问题、置信度和草稿状态。

### 7.4 Prompt 硬约束

- 只使用提供的工单内容、有效配置、合同、批准材料和历史记录；
- 每项结论标明依据来源；
- 不知道时输出「证据不足」；
- **不得补造现场事实**；
- **不得自行发布配置**；
- **不得形成正式核查结论**；
- **不得修改账目、扣款、付款或生成付款指令**；
- 输出**始终为 `draft`**，由人员确认后才能进入下一步。

### 7.5 资料整理禁令

无论由 AI 生成还是人工修改，资料整理结果**不得**：

- 用套餐长度替代实际完工长度；
- 将材料和安装数量错误相加；
- 根据其他工单推断本单供料方；
- 填写输入中没有的施工事实。

资料整理结果必须包含原始内容、提取的项目/数量/单位/供料方/服务范围、每项内容的来源位置、缺失事实和待确认问题，以及 AI 草稿 / 人工修改 / 正式版本的区分。

---

## 8. 技术栈（锁定，不可变更）

| 层 | 技术 |
| --- | --- |
| 前端框架 | Vite + React 18 |
| 语言 | TypeScript |
| 样式 | Tailwind CSS v3 |
| 路由 | React Router v6 |
| 前端托管 | Cloudflare Pages |
| 数据库 | Cloudflare D1 |
| 后端 | Cloudflare Workers |

> 以上技术栈已锁定，未经确认不得更换或引入替代方案。

**AI 能力提供方**：具体提供方待「锁定外部依赖」阶段确定（优先评估 Cloudflare Workers AI，效果不足再考虑外部 API）。不开发独立 Skill。

---

## 9. 目录结构约定

```
src/features/               # 按功能模块划分
  orders/                   # 工单中心（跨系统工单聚合）
  reconciliation/           # 双账业务对账
  fee-library/              # 接入配置（字段映射、对应与计算规则）
  submit/                   # 资料交接（三种资料入口）
  settlement/               # 对账结果与京东正式账单引用
  dashboard/                # 数据看板（非 MVP，暂不投入）
src/shared/                 # 公共组件与工具
functions/api/              # Cloudflare Workers 后端
memory-bank/                # 项目记忆文件夹
docs/                       # 需求文档
```

- 新功能一律放入 `src/features/` 下对应的模块目录。
- 跨模块复用的组件与工具放入 `src/shared/`。
- 后端逻辑统一放在 `functions/api/`。

> 模块名对应关系依据需求基线 §14。开发阶段如确认需要新增模块（如资料交接/异常待办、AI 辅助接口），须在此处登记后再建目录。

**金额约定**：所有金额字段一律以「分」为单位存 INTEGER，前端显示时 `/100` 转元。

---

## 10. 禁区（绝对禁止）

以下操作在任何情况下都**不得**执行。

### 10.1 数据与记录边界

1. **不得覆盖原始记录。** 严格遵守 §6.2 记录层级，后一层级不得覆盖前一层级。
2. **不得修改服务商应收、京东账单**，不得扣款、付款或生成付款指令。
3. **京东账单模块只读。**
4. **不得访问服务商本地成本、工资、其他客户和经营数据。**
5. **不得访问本平台以外的任何系统数据库**（含京东其他业务系统与 C 端消费者数据）。外部数据一律只读接入。
6. **不得冒充原系统正式记录。** 新后台只保存新增的过程记录。

### 10.2 AI 边界

7. AI 输出**只能是草稿**；无依据必须输出「证据不足」，**不得编造事实**。
8. **不得由 AI 形成正式核查结论、发布配置、修改账目或生成付款指令。**
9. **不开发独立 Skill、复杂 Agent、RAG 或向量数据库。**
10. 确定性转换、校验、计算、幂等**必须由普通程序完成**，不得交给 AI。

### 10.3 流程与审批

11. **正式提交前必须经过人员确认和原系统校验。**
12. **不得因接入新后台而新增任何审批要求。**
13. 重复交付**不得产生重复正式结果**；较早配置版本**不得覆盖**后来已确认记录。
14. MVP 中真实接口可由**清晰标注的数据适配层和模拟数据**代替，但**不得伪装成已经接通真实生产系统**。

### 10.4 工程约束

15. **不得修改 [src/config/schema.ts](src/config/schema.ts)** —— 该文件是**版本化配置模型的核心结构**，改动前必须确认。（具体字段结构留待「从定稿原型反推数据库字段」阶段再设计，本次不动。）
16. **不得安装未确认的 UI 库** —— 引入任何 UI 组件库前必须先征得确认。**AI 模型与 SDK 依赖同样适用本条。**
17. **不得实现超出 MVP 范围的能力**：现场工人端与移动端；人员排班、勘测、施工进度、库存、领退料；新的施工验收状态机；新的变更审批流；京东正式账单生成/调整/结算/付款；自动责任认定；完整 BI 看板。

---

## 11. 工作约定

- 提交前先核对是否触及以上禁区。
- 涉及 [src/config/schema.ts](src/config/schema.ts)、UI 库选型、AI 提供方、数据库访问范围的改动，**必须先向用户确认**。
- 需求文档写入 `docs/`，项目记忆与进度写入 `memory-bank/`。
- `memory-bank/` 中 1.0 时期的内容属于历史资料；与需求基线冲突时，以需求基线为准。
- 产品原则（三类状态独立、双账独立、原始记录不覆盖、AI 只出草稿）**不得在实现中合并或简化**。

---

## 12. 模块文档索引

模块级 AGENTS.md 随对应功能开发时逐步在 `src/features/<模块>/AGENTS.md` 补充。**每新增一个子文件，必须回来在这里加一行索引。**

> 当前为空，开发阶段逐步补充。
